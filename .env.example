# Secret token for docs URL. When set, the site is served at /<token>/
# instead of /. Generate one with: pnpm docs:generate-token
# Or create manually: DOCS_SECRET_TOKEN=<your-random-hex-string>
DOCS_SECRET_TOKEN=
